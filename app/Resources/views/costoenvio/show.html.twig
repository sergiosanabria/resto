{% extends '::base.html.twig' %}

{% block body %}
    <h1>CostoEnvio</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ costoEnvio.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ costoEnvio.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ costoEnvio.descripcion }}</td>
            </tr>
            <tr>
                <th>Precio</th>
                <td>{{ costoEnvio.precio }}</td>
            </tr>
        </tbody>
    </table>

    <div class="form-buttons">

            <a class="btn btn-default" href="{{ path('costoenvio_index') }}">Volver </a>

            <a class="btn btn-default" href="{{ path('costoenvio_edit', { 'id': costoEnvio.id }) }}">Editar</a>

            {{ form_start(delete_form) }}
                <input type="submit" class="btn btn-danger" value="Delete">
            {{ form_end(delete_form) }}
    </div>
{% endblock %}
